#!/usr/bin/env node
// UserPromptSubmit 훅 — 박코가 말을 걸 때마다 대조 항목을 눈앞에 띄운다.
//
// 박코 2026-09-20: "그래서 규칙 꺼내볼 순간은 제대로 셋팅 했냐? 안한 거 같은데"
// 규칙 파일은 세션 처음에 한 번 읽히고 끝난다. 답을 쓰는 순간에는 대조되지 않는다.
// 그래서 매 턴 여기서 다시 띄운다. 짧게 — 길면 토큰만 먹고 안 읽힌다.
//
// AI 를 안 부른다 — 돈이 들지 않는다.
const 대조 = [
  "[보내기 전 대조 — 박코 규칙]",
  "① 박코가 손으로 할 일이 들어가면: 전부 세었나 · 번호 매겼나 · 링크와 단추 이름과 칸 이름과 넣을 값이 다 있나 · 못 본 화면을 지어내지 않았나. 하나라도 아니면 보내지 않는다.",
  "② 지적을 받았으면 두 층으로: (가) 이 건 (나) 다시 안 나게 하는 구조. (나) 없이 (가)만 내면 끝난 답이 아니다. 두 번째 지적이면 (나)부터.",
  "③ 안 한 것을 했다고 하지 않는다. 확인한 것만 됐다고 한다.",
].join("\n");

process.stdout.write(JSON.stringify({
  hookSpecificOutput: { hookEventName: "UserPromptSubmit", additionalContext: 대조 },
  suppressOutput: true,
}));
